apiVersion: v1
kind: Service
metadata:
  name: brokerage-order-service
  labels:
    app.kubernetes.io/name: brokerage-order-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: brokerage-order-service
  ports:
    - name: http
      port: 80
      targetPort: http
